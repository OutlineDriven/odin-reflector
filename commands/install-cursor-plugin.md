---
name: install-cursor-plugin
description: Install codex-reflector hook export into Cursor-compatible Claude settings.
---

Install codex-reflector into Cursor compatibility settings for this machine:

```bash
scripts/install-cursor.sh
```

Install into a specific project checkout:

```bash
scripts/install-cursor.sh "$ARGUMENTS"
```
